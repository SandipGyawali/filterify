type SUB_CATEGORIES_TYPE = {
  name: string;
  selected: boolean;
  href: string;
};

type OPTIONS_SORT_TYPE = { name: string; value: string };

export type { SUB_CATEGORIES_TYPE, OPTIONS_SORT_TYPE };
